memlounge
=========
This is old code - It was meant to be a fast proxy to memcached and couchdb.
My initial tests were very promising
  - the efficient network code was so efficient that the memcached tests
  were FASTER via my proxy than directly to memcached on localhost
